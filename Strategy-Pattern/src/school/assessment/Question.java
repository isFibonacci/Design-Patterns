package school.assessment;

/*This interface has a function mark(String ans) that returns true
if its parameter is the correct answer, false if not*/
public interface Question {
    public boolean mark(String ans);
}
